import { CardColor, CardValue } from "../../model/card.interface";
import { ITable } from "../../model/table.interface";

describe('blackbox normality', () => {

    it('should work with an initial state (4 Players)', () => {
        
    });

    it('should work mid rounds (4 Players)', () => {

    });

});

describe('round end', () => {

});

describe('end state after last round', () => {

});
