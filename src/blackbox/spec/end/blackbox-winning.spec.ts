describe('a round ends when every player bets the same amount', () => {

});

describe('a table ends when flop + turn + river lay on the table and every player bets the same amount', () => {

    it('should evaluate obvious wins', () => {});

    it('should evaluate split pots', () => {});

    it('should evaluate side pots', () => {});

});
